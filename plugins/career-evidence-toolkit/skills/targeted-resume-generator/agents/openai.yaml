interface:
  display_name: Targeted Resume Generator
  short_description: Create truthful resumes for a role title or specific vacancy
  default_prompt: Use $targeted-resume-generator to create a targeted resume for this role.
policy:
  allow_implicit_invocation: true
